import { MarketData } from './market-data';

export class Market {
    id:number;
    partnerID:number;
    market:String;
    lang: string;
    brand: string;
    sites: string;
    isActive: boolean;
}